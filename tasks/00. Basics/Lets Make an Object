Set the three properties on person.
--
var person = {

};


assert(person.name === 'bob', 'name should equal "bob"');
assert(person.gender === 'male', 'gender should equal "male"');
assert(person.weight === 100, 'weight should equal 100');
